name: dxs_tunnel
description: Application VPN UDP DXS Tunnel fonctionnelle.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # Moteur de tunnel VPN UDP natif
  wireguard_flutter: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
